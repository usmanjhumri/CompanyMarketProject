import { createSlice } from "@reduxjs/toolkit";

const initialState = {
  isLoading: false,
  catergories: {},
};

const homeSlice = createSlice({
  name: "home",
  initialState,
  reducers: {
    
  },
});
